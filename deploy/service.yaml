---
apiVersion: v1
kind: Service
metadata:
  name: weeeeeiserbikes-wordpress
  namespace: weeeeeiserbikes-wordpress
spec:
  type: ClusterIP
  selector:
    app: weeeeeiserbikes-wordpress
  ports:
    - name: http
      port: 80
      targetPort: http
