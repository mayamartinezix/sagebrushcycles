---
apiVersion: v1
kind: Service
metadata:
  name: weeeeeiserbikes-decap
  namespace: weeeeeiserbikes-decap
spec:
  type: ClusterIP
  selector:
    app: weeeeeiserbikes-decap
  ports:
    - name: http
      port: 80
      targetPort: http
