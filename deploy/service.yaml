---
apiVersion: v1
kind: Service
metadata:
  name: weeeeeiserbikes-grav
  namespace: weeeeeiserbikes-grav
spec:
  type: ClusterIP
  selector:
    app: weeeeeiserbikes-grav
  ports:
    - name: http
      port: 80
      targetPort: http
