---
apiVersion: v1
kind: Service
metadata:
  name: weeeeeiserbikes
  namespace: weeeeeiserbikes
spec:
  type: ClusterIP
  selector:
    app: weeeeeiserbikes
  ports:
    - name: http
      port: 80
      targetPort: http
